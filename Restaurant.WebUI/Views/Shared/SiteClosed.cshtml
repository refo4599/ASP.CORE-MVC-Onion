@{
    ViewData["Title"] = "Closed Now";
}

<style>
    body {
        margin: 0;
        padding: 0;
        height: 100vh;
        font-family: 'Cairo', sans-serif;
        overflow: hidden;
        background: linear-gradient(135deg, #f5f5f5, #444);
        background-size: 400% 400%;
        animation: gradientShift 10s ease-in-out infinite;
        position: relative;
    }

    /* 🔹 حركة الخلفية بين درجات الرمادي */
    @@keyframes gradientShift {
        0% {
            background: linear-gradient(135deg, #f5f5f5, #555);
        }

        25% {
            background: linear-gradient(135deg, #e0e0e0, #333);
        }

        50% {
            background: linear-gradient(135deg, #fafafa, #666);
        }

        75% {
            background: linear-gradient(135deg, #dcdcdc, #222);
        }

        100% {
            background: linear-gradient(135deg, #f5f5f5, #555);
        }
    }

    body::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.25);
        z-index: 1;
    }

    .closed-container {
        position: relative;
        z-index: 2;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .closed-box {
        /* 🎨 نفس تصميم البوكس في صفحات Login/Register */
        background: linear-gradient(135deg, #6a11cb, #2575fc);
        und: rgba(255, 255, 255, 0.12);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border-radius: 20px;
        padding: 50px 40px;
        color: #fff;
        text-align: center;
        width: 90%;
        max-width: 600px;
        box-shadow: 0 0 40px rgba(0, 0, 0, 0.3);
        animation: fadeIn 1.2s ease forwards;
    }

    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(40px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .closed-title {
        font-size: 2.3rem;
        font-weight: 800;
        color: #ff1744; /* 🔴 أحمر واضح */
        text-shadow: 0 0 10px rgba(255, 23, 68, 0.6);
        margin-bottom: 20px;
    }

    .closed-text {
        font-size: 25px; /* ✨ تكبير الخط */
        line-height: 1.6;
        color: #f1f1f1;
    }

    .menu-btn {
        background: linear-gradient(90deg, #ff1744, #2979ff);
        border: none;
        border-radius: 40px;
        padding: 12px 35px;
        color: white;
        font-weight: bold;
        font-size: 1.5rem;
        text-decoration: none;
        display: inline-block;
        box-shadow: 0 0 15px rgba(255, 255, 255, 0.1);
        position: relative;
        overflow: hidden;
        transition: all 0.3s ease;
        margin-top: 20px;
    }

        .menu-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 0 25px rgba(255, 255, 255, 0.3);
        }

        .menu-btn::after {
            content: "";
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.2);
            transform: skewX(-20deg);
            transition: 0.5s;
        }

        .menu-btn:hover::after {
            left: 100%;
        }
</style>

<div class="closed-container">
    <div class="closed-box">
        <h1 class="closed-title">🔒 المطعم مغلق الآن</h1>
        <p class="closed-text mb-3">ساعات العمل من <strong>6:00 صباحًا</strong> إلى <strong>12:00 منتصف الليل</strong>.</p>
        <p class="closed-text mb-4">يمكنك تصفح المنيو فقط في الوقت الحالي ❤️</p>
        <a href="/Menu" class="menu-btn">🍽️ تصفح المنيو الآن</a>
    </div>
</div>
